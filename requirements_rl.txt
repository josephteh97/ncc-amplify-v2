# OpenClaw-RL dependencies
# Install with: pip install -r requirements_rl.txt

# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Async HTTP (Ollama calls)
httpx>=0.27.0
aiohttp>=3.9.0

# Data / serialisation
pydantic>=2.7.0

# Logging
loguru>=0.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Existing pipeline deps (already in requirements.txt — listed for completeness)
# pymupdf
# pillow
# numpy
